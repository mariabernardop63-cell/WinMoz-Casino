kind = "web"
  previewPath = "/"
  title = "WinMoz"
  version = "1.0.0"
  id = "artifacts/winmoz"
  router = "path"

  [[integratedSkills]]
  name = "react-vite"
  version = "1.0.0"

  [[services]]
  name = "web"
  paths = [ "/" ]
  localPort = 8081

  [services.development]
  run = "pnpm --filter @workspace/winmoz run dev"

  [services.production]
  build = [ "pnpm", "--filter", "@workspace/winmoz", "run", "build" ]
  publicDir = "artifacts/winmoz/dist/public"
  serve = "static"

  [[services.production.rewrites]]
  from = "/*"
  to = "/index.html"

  [services.env]
  PORT = "8081"
  BASE_PATH = "/"
